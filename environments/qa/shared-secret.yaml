apiVersion: v1
kind: Secret
metadata:
  name: money-moving-shared-secret
  namespace: money-moving
type: Opaque
data: {}
  # Add secrets as needed in base64 format
  # Example: DATABASE_URL: <base64-value>
