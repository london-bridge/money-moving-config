apiVersion: v1
kind: Secret
metadata:
  name: money-moving-shared-secret
  namespace: money-moving
type: Opaque
data:
  # Empty secret - add keys as needed
  # Example: DATABASE_URL: <base64-encoded-value>
